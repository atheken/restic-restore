version: "3.0"
services:
  main:
    build: ./main
    command: sleep infinity
    environment:
      RESTIC_CACHE_DIR: /restic-cache
    volumes:
      - ../:/data
      - ../configs:/configs
      - /tmp/restic-cache:/restic-cache
